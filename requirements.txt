# ── AI Meeting Assistant ── requirements.txt ──────────────────────────────────

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2026.3.17      # Download audio from YouTube URLs
curl-cffi               # Required by yt-dlp for browser impersonation (bypassing 403)
pydub                     # Audio file manipulation / format conversion
audioop-lts                         # Drop-in replacement for audioop (removed in Python 3.13)
ffmpeg-python            # FFmpeg bindings (FFmpeg binary must be installed separately)

# ── Speech-to-Text (local Whisper) ────────────────────────────────────────────
openai-whisper          # OpenAI Whisper (runs locally)
torch                      # PyTorch backend for Whisper
torchvision                # Required by some PyTorch models
torchaudio                 # Audio utilities for PyTorch

# ── Translation (Hindi → English) ─────────────────────────────────────────────
deep-translator           # Lightweight translation wrapper (Google backend)

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain
langchain-core
langchain-community
langchain-mistralai        # LangChain wrapper for Mistral API
mistralai                  # Official Mistral Python client

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
chromadb                   # Local vector store
langchain-chroma           # LangChain integration for ChromaDB
sentence-transformers      # HuggingFace embedding models
langchain-huggingface      # LangChain ↔ HuggingFace embeddings bridge
huggingface-hub            # Model downloading from HuggingFace Hub
tiktoken                   # Token counting for text splitting

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit
streamlit-extras           # Additional Streamlit widgets / utilities
watchdog                   # File-change watcher (improves Streamlit hot-reload)

# ── PDF & TXT Export ──────────────────────────────────────────────────────────
reportlab                  # PDF generation
fpdf2                      # Lightweight alternative PDF library

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv              # Load API keys from .env
numpy                      
tqdm                       # Progress bars
requests